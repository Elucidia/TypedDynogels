import expect from 'chai';
import {Table} from './table';

describe('Testing table', () => {
    let table: Table;

    beforeEach(() => {
        // for mock, but in typescript it seems to be difficult
    });

    describe('#getItem', () => {
        it('should get item by hash key', async () => {

        });
    })
})
